package sineSection.greenshift.launcher;

//TODO: Add 2+ launcher options (Vanilla, Modded, Modded with only certain mods enabled)
/**
 * A Game Launcher class Resposible for loading a save file, as well as the data
 * into the fetch classes.
 * 
 * <p>
 * Must allow the user to select which biome file they wish to use for the ship,
 * and the mods to launch with Then, loads the necessary data into the run
 * classes before starting the game and loading the ship
 *
 */
public class Launcher {

	
	public void launch() {
		//TODO: iterate through all data folders, then put the data into the appropriate class through the necessary parser.
	}
}
